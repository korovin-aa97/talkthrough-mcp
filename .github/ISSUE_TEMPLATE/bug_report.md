---
name: Bug report
about: Something broke or behaved wrong
labels: bug
---

## What happened

<!-- What did you run / ask your agent to do, and what went wrong? -->

## Expected

## Environment

- OS + arch (e.g. macOS 15 arm64 / Ubuntu 24.04 x64):
- Install method (uvx git+https / uvx PyPI / local checkout):
- MCP client (Claude Code / Claude Desktop / Cursor / Cline / other):
- `TALKTHROUGH_WHISPER_MODEL` (if set):

## Job evidence (for processing/retrieval bugs)

<!-- From the summary or ~/.talkthrough/jobs/<job_id>/manifest.json -->

- `job_id`:
- `tool_versions` block from the manifest:
- media kind/duration (no need to share the recording itself — it never
  leaves your machine, and please don't attach private recordings):

## Logs

<!-- Server stderr / CLI output around the failure -->
